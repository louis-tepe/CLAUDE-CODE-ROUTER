# Operational config only — provider config is in proxy-modes/*.env
# API keys and provider URLs are injected by claude-shell.sh at proxy startup
PORT=8082
LOG_LEVEL=INFO
# CONNECT_TIMEOUT=10
# READ_TIMEOUT=300
# MAX_CONNECTIONS=100
# CIRCUIT_BREAKER_THRESHOLD=5
# CIRCUIT_BREAKER_RECOVERY=120
